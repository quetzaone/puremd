{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal local Markdown viewer permissions",
  "windows": ["main"],
  "permissions": ["core:default"]
}
